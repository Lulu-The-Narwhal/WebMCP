{
  "name": "webmcp-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "dev": "wrangler pages dev public --compatibility-date=2026-08-27",
    "deploy": "wrangler pages deploy public --project-name=webmcp"
  },
  "dependencies": {
    "lulu-ads": "^0.9.5"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0",
    "@cloudflare/workers-types": "^5.0.0",
    "typescript": "^5.5.0"
  }
}
